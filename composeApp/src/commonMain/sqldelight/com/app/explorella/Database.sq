CREATE TABLE bucketEntry (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  title TEXT NOT NULL,
  description TEXT,
  latitude REAL,
  longitude REAL
);

insertBucketEntry:
INSERT INTO bucketEntry (title, description, latitude, longitude)
VALUES (?, ?, ?, ?);

selectAllBucketEntries:
SELECT * FROM bucketEntry;